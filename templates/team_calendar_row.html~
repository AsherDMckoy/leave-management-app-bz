<!-- leave_row.html -->
<tr data-id>
  <td>{{ leave.name }}</td>
  <td>{{ leave.start_date }}</td>
  <td>{{ leave.end_date }}</td>
  <td>{{ leave.days }}</td>
  <td>{{ leave.leave_type }}</td>
    <td>
    {% if let Some(comment) = leave.comments %}
    {% if !comment.is_empty() %}
    {{ comment }}
    {% endif %}
    {% endif %}
  </td>
 </tr>
