<!-- team_prev_row.html -->
<div class="team_prev_details__row flex">
  <div class="team_prev_details__nameInfo flex col">
    <span class="team_prev_datails__name">{{ leave.name }}</span>
    <div class="team_prev_details__dates subtext" data-start="{{ leave.start_date }}" data-end="{{ leave.end_date }}"></div>
  </div>
  <div class="team_prev_details__status flex col">
    <span class="team_prev_details__leave_status subtext2">{{ leave.current_status }}</span>
    <span class="team_prev_details__leave_days subtext">
      {{ leave.days }}
      {% if leave.current_status == "planned" %}
      {{ "days to go"  }}
      {% else %}
      {{ "days left" }}
      {% endif %}
    </span>
  </div>
</div>
